// function that takes (1) param "name"
// returns an object similar to PERSON object from fund. ii
// RETURNED OBJECT contains:
//    name : param = {name : param}
//
function personConstructor(name) {
  var person = {
    name : "name"
  }
}
console.log(personConstructor(Bob));
